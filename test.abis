@main[]->{
	
    PrintL: @Factorial[5];
    
    End::;
}

@Factorial[num:num]->num{
    Set:num|i;
    Eql:$i|1;
    Set:num|rslt;
    Eql:$rslt|1;

    Loop:
        Eql:$rslt|($rslt * $i);
    LoopEnd:
        Eql:$i|($i + 1);
        If:($i < $num)|Loop;

    Return: $rslt;
}